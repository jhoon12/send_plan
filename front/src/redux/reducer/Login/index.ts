import LoginAction, {setID, setPW} from '../../actions/Login/index';

interface InitialInterface{
    id : string,
    pw : string
}

const initialState : InitialInterface = {
    id:'',
    pw:''
}

const LoginState = ()=>{
    
}